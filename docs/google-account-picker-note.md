# Google account picker

The first-run screen uses Android/Google Play services' account picker to show Google accounts that already exist on the device. The app stores only the email address the user explicitly selects. No password is requested or stored.
